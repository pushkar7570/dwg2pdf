Backend API: uvicorn main:app --reload --host 0.0.0.0 --port 8000
Frontend API: streamlit run app.py --server.port 8501 --server.address 0.0.0.0
Health Check: # Terminal 3 - Quick test
curl -s http://localhost:8000/health | python -m json.tool

curl -X POST "http://localhost:8000/process" \
  -F "file=@/workspaces/dwg2pdf/tests/sample_files/sample.dxf" \
  --output /tmp/corrected.pdf \
  -w "\nStatus: %{http_code} | Time: %{time_total}s\n"

  